{
  "name": "svg-coordinate-map",
  "version": "1.1.01",
  "description": "Map coordinate from viewport to viewBox",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha ./test",
    "test:watch": "npm run test --watch",
    "coverage": "nyc --check-coverage --statements 100 --branches 100 --lines 100 --reporter=lcov --reporter=text npm run test && nyc report --reporter=text-lcov",
    "coveralls": "npm run coverage | coveralls"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tnptop/svg-coordinate-map.git"
  },
  "keywords": [
    "svg",
    "viewBox",
    "viewport",
    "coordinate"
  ],
  "author": "Thanapat Bhunnachet",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/tnptop/svg-coordinate-map/issues"
  },
  "homepage": "https://github.com/tnptop/svg-coordinate-map#readme",
  "dependencies": {
    "big.js": "^5.1.2"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "coveralls": "^3.0.1",
    "mocha": "^5.2.0",
    "nyc": "^12.0.2",
    "proxyquire": "^2.0.1",
    "sinon": "^6.0.0"
  }
}
