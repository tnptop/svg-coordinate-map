{
  "name": "svg-coordinate-map",
  "version": "1.1.2",
  "description": "Map coordinate from viewport to viewBox",
  "main": "lib/index.js",
  "scripts": {
    "test": "mocha ./test",
    "test:watch": "npm run test --watch",
    "coverage": "nyc --check-coverage --statements 100 --branches 100 --lines 100 --reporter=lcov --reporter=text npm run test",
    "coveralls": "npm run coverage && nyc report --reporter=text-lcov | coveralls"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tnptop/svg-coordinate-map.git"
  },
  "keywords": [
    "svg",
    "viewBox",
    "viewport",
    "coordinate"
  ],
  "author": "Thanapat Bhunnachet",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/tnptop/svg-coordinate-map/issues"
  },
  "homepage": "https://github.com/tnptop/svg-coordinate-map#readme",
  "dependencies": {
    "big.js": "^5.2.2"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "coveralls": "^3.0.11",
    "mocha": "^7.1.1",
    "nyc": "^15.0.1",
    "proxyquire": "^2.1.3",
    "sinon": "^9.0.1"
  }
}
